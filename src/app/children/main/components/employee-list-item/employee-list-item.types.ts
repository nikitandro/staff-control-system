import { Input } from '@angular/core';

export interface IEmployeeInfo {
    firstname?: string;
    lastname?: string;
    patronymic?: string;
    avatarUrl?: string;
    department?: string;
    post?: string;
    salary?: string;
}
